@model BlogListResponseModel
@{
    ViewData["Title"] = "BlogHttpClient";
    int count = 0;
}

<h4>
    <a href="/bloghttpclient/create" class="btn btn-success"><i class="fa-solid fa-plus"></i></a> Blog List
</h4>

<table class="table">
    <thead>
        <tr>
            <th scope="col">#</th>
            <th scope="col">Title</th>
            <th scope="col">Author</th>
            <th scope="col">Content</th>
            <th scope="col">Action</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model.Data)
        {
            <tr>
                <th scope="row">@(++count)</th>
                <td>@item.Blog_Title</td>
                <td>@item.Blog_Author</td>
                <td>@item.Blog_Content</td>
                <td>
                    <a class="btn btn-warning" href="/bloghttpclient/edit?id=@item.Blog_Id">
                        <i class="fa-solid fa-pen-to-square"></i>
                    </a>
                    <a class="btn btn-danger" href="/bloghttpclient/delete?id=@item.Blog_Id">
                        <i class="fa-solid fa-trash"></i>
                    </a>
                </td>
            </tr>
        }

    </tbody>
</table>